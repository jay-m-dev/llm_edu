# Tokenizer Prototype

A minimal, standalone tokenizer prototype for the LLM simulator with pseudo-embeddings and attention weights.

## Run
Open `prototype/index.html` in a browser.

## Test
From the repo root:
```
npm test
```
